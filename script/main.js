var delay = 5000;setInterval(move, delay);function move() {    var pre = document.getElementById('hello-keshee');    var sx = 1, sy = 1;    var tx = 0, ty = 0;    var z = 0;    sx += r(0.03);    sy += r(0.03);    tx += r(4);    ty += r(4);    z += r(.8);    // console.log('move', sx, sy, tx, ty, z);    pre.style.transform = [scaleStr(sx, sy), translateStr(tx, ty), rotateStr(z)].join(' ');}function scaleStr(x, y) {    return "scale3d(" + [x, y, 1] + ")";}function translateStr(x, y) {    return "translate3d(" + [x+"px", y+"px", 0] + ")";}function rotateStr(z) {    return "rotateZ(" + z + "deg)";}function r(max) {    return Math.random() * max;}